node_modules/
.env
.env.local
.DS_Store

# Local Admin API credentials minted by `spree api` / `spree auth`
.spree/

# Kamal secrets (contain passwords and tokens)
.kamal/secrets

# Backend environment
backend/.env
